import post from '../../schemas/post'
import page from '../../schemas/page'
import { heroBlock, textContentBlock, statsBlock, faqBlock } from '../../schemas/blocks'

export const schemaTypes = [
  post,
  page,
  heroBlock,
  textContentBlock,
  statsBlock,
  faqBlock,
]


