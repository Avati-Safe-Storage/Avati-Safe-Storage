{
  "name": "avati-safe-storage-workspace",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run dev --prefix avati-website",
    "dev:admin": "npm run dev --prefix avati-admin-portal",
    "dev:portal": "npm run dev --prefix avati-customer-portal",
    "build": "npm run build --prefix avati-website",
    "build:admin": "npm run build --prefix avati-admin-portal",
    "build:portal": "npm run build --prefix avati-customer-portal",
    "install:all": "npm install --prefix avati-website && npm install --prefix avati-admin-portal && npm install --prefix avati-customer-portal"
  }
}
